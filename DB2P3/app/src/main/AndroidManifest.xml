<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="downing.pearce.db2p3">

    <!-- Need to use the INTERNET!
         https://www.tutorialspoint.com/android/android_php_mysql.htm       -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Index">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Query activities -->
        <activity android:name=".AuthorPurch_Q1" >
        </activity>

        <activity android:name=".InputAuthors_Q2" >
        </activity>

        <activity android:name=".InputCustomers_Q3" >
        </activity>

        <activity android:name=".InputTitle_Q4" >
        </activity>

        <activity android:name=".BestSellers_Q5" >
        </activity>
    </application>

</manifest>